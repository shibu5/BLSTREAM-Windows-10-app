<Page
    x:Class="Pat_1.View.ImageList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pat_1.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

       <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" >
        <ListView x:ConnectionId='1' x:Name="Lista_obrazkow" HorizontalAlignment="Left" Margin="0,1,0,0" VerticalAlignment="Top" ItemsSource="{Binding lista}" SelectedItem="{Binding selecteditem,Mode=TwoWay}"                                             >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0" HorizontalAlignment="Center">
                        <Border Margin="0,1,0,0" BorderThickness="2" Height="{Binding wysokosc1}" Width="{Binding szerokosc1}" Background="{x:Null}">
                            <Border.BorderBrush>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FF6A3F3F" Offset="0"/>
                                    <GradientStop Color="#FFE04D4D" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Image Source="{Binding Image}"/>
                        </Border>
                        <Border Margin="2,1,0,0" BorderThickness="2" Height="{Binding wysokosc1}" Width="{Binding szerokosc2}">
                            <Border.BorderBrush>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FF6A3F3F" Offset="0"/>
                                    <GradientStop Color="#FFE04D4D" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <TextBlock Text="{Binding Nazwa}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="Auto" Height="Auto"/>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

